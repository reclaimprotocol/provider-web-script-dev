declare global {
  /**
   * If a website has a custom type, you declare it here.
   *
   * For example:
   *
   * ```ts
   * interface User {
   *   name: string;
   *   age: number;
   * }
   * interface Window {
   *   User: User;
   * }
   * ```
   */
}

export {};
